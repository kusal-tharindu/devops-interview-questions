{
  "name": "devops-interview-questions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate:stats": "node scripts/generate-topic-stats.js",
    "generate:cards": "node scripts/parse-cards.js",
    "prestart": "npm run generate:stats && npm run generate:cards",
    "prebuild": "npm run generate:stats && npm run generate:cards",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "serve": "docusaurus serve",
    "clear": "docusaurus clear",
    "validate:cards": "node scripts/parse-cards.js"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.2",
    "@docusaurus/preset-classic": "3.10.2",
    "@mdx-js/react": "3.1.1",
    "clsx": "2.1.1",
    "prism-react-renderer": "2.4.1",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.10.2",
    "@docusaurus/types": "3.10.2"
  },
  "engines": {
    "node": ">=20.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
